<UserControl x:Class="WassControlSys.Views.ActualizacionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Actualizaciones de Software" Style="{StaticResource HeaderTextStyle}"/>
            <TextBlock Text="Gestione las actualizaciones de sus aplicaciones instaladas mediante Winget." Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <Button Content="Buscar Actualizaciones" Command="{Binding RefreshUpdatableAppsCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Actualizar Todo" Command="{Binding UpdateAllAppsCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>

            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15">
                <DataGrid ItemsSource="{Binding UpdatableApps}" AutoGenerateColumns="False" Background="Transparent" Foreground="White" BorderThickness="0" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Versión Actual" Binding="{Binding CurrentVersion}" Width="150"/>
                        <DataGridTextColumn Header="Nueva Versión" Binding="{Binding AvailableVersion}" Width="150"/>
                        <DataGridTemplateColumn Header="Acción" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Button Content="Actualizar" Command="{Binding DataContext.UpdateAppCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding Id}" Padding="5,2">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsUpdating}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <StackPanel VerticalAlignment="Center">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsUpdating}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="{Binding UpdateStatusMessage}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                            <ProgressBar Value="{Binding UpdateProgress}" Maximum="100" Height="6" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
