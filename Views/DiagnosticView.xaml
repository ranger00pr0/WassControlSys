<UserControl x:Class="WassControlSys.Views.DiagnosticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="0,0,0,0">
        <TextBlock Text="Módulo de Diagnóstico" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,30"/>
        
        <WrapPanel Orientation="Horizontal">
             <!-- SFC -->
            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,20,20" Width="300" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Comprobador de Archivos (SFC)" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock Text="Analiza la integridad de todos los archivos del sistema protegidos y repara los archivos con problemas." Foreground="#AAA" TextWrapping="Wrap" Margin="0,0,0,15" Height="80"/>
                    <Button Content="Ejecutar SFC /scannow" Command="{Binding RunSfcCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                </StackPanel>
            </Border>

             <!-- DISM -->
            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,20,20" Width="300" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Reparación de Imagen (DISM)" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock Text="Repara la imagen de Windows para corregir errores que SFC no puede solucionar." Foreground="#AAA" TextWrapping="Wrap" Margin="0,0,0,15" Height="80"/>
                    <Button Content="Ejecutar DISM" Command="{Binding RunDismCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                </StackPanel>
            </Border>

             <!-- CHKDSK -->
            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,20,20" Width="300" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Check Disk (CHKDSK)" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock Text="Comprueba el sistema de archivos en busca de errores lógicos y físicos." Foreground="#AAA" TextWrapping="Wrap" Margin="0,0,0,15" Height="80"/>
                    <Button Content="Programar CHKDSK" Command="{Binding RunChkdskCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                </StackPanel>
            </Border>
        </WrapPanel>
    </StackPanel>
</UserControl>
