<UserControl x:Class="WassControlSys.Views.RendimientoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:WassControlSys.Core"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1000">
    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedProcesses" Source="{Binding Processes}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="IsForeground"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>

        <core:BooleanToStringConverter x:Key="IsForegroundToTypeConverter"/>
    </UserControl.Resources>
    
    <Grid Style="{StaticResource AnimatedViewStyle}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Rendimiento y Procesos" Style="{StaticResource HeaderTextStyle}"/>

        <!-- Dashboard Moderno de Métricas -->
        <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,20">
            <!-- CPU -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="5">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Procesador (CPU)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Grid>
                        <Ellipse Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="4" Width="80" Height="80" Opacity="0.1"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding CpuTempC, StringFormat={}{0:F0}°C}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Temp" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="{Binding ThermalAlert}" Foreground="#EF4444" FontSize="10" Margin="0,8,0,0" TextAlignment="Center" TextWrapping="Wrap"
                               Visibility="{Binding ThermalAlert, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                </StackPanel>
            </Border>

            <!-- RAM -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="5">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Memoria (RAM)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Grid>
                        <Ellipse Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="4" Width="80" Height="80" Opacity="0.1"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding RamUsage, StringFormat={}{0:0}%}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Uso" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="En uso" Foreground="#888" FontSize="10" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- CPU Cores (Mini View) -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="5">
                <StackPanel>
                    <TextBlock Text="Uso por Núcleos" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <ItemsControl ItemsSource="{Binding CpuPerCore}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2">
                                    <ProgressBar Value="{Binding Usage}" Maximum="100" Height="4" VerticalAlignment="Bottom" Foreground="{DynamicResource PrimaryBrush}"/>
                                    <TextBlock Text="{Binding Usage, StringFormat={}{0:F0}%}" FontSize="9" Foreground="White" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Index, StringFormat={}C{0}}" FontSize="9" Foreground="#666" HorizontalAlignment="Left" Margin="0,0,0,4"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Red -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="5">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Red (Descarga)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Grid>
                        <Ellipse Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="4" Width="80" Height="80" Opacity="0.1"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding NetRecvMbps, StringFormat={}{0:F1}}" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Mbps" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="↓ Entrada Red" Foreground="#888" FontSize="10" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Disco -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="5">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Impacto Disco" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Grid>
                        <Ellipse Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="4" Width="80" Height="80" Opacity="0.1"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding DiskWritesPerSec, StringFormat={}{0:F0}}" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="I/O" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="Escrituras/s" Foreground="#888" FontSize="10" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">
            <!-- Procesos -->
            <TabItem Header="Procesos Activos">
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="0,12,12,12" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                            <!-- Barra de búsqueda y botón actualizar -->
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Cuadro de búsqueda -->
                            <Border Grid.Column="0" 
                                    Background="{DynamicResource WindowBackgroundBrush}" 
                                    BorderBrush="{DynamicResource SecondaryTextBrush}" 
                                    BorderThickness="1" 
                                    CornerRadius="6"
                                    Padding="10,8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Width" Value="200"/>
                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="300" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="200" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Ícono de búsqueda -->
                                    <TextBlock Grid.Column="0" 
                                               Text="🔍" 
                                               FontSize="14" 
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>
                                    
                                    <!-- TextBox de búsqueda -->
                                    <TextBox Grid.Column="1" 
                                             Text="{Binding ProcessSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{DynamicResource TextBrush}"
                                             VerticalAlignment="Center"
                                             FontSize="13">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Style.Triggers>
                                                    <Trigger Property="Text" Value="">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                                    <VisualBrush.Visual>
                                                                        <TextBlock Text="Buscar proceso por nombre o PID" 
                                                                                   Foreground="White" 
                                                                                   FontSize="13"
                                                                                   Opacity="0.6"/>
                                                                    </VisualBrush.Visual>
                                                                </VisualBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    
                                    <!-- Botón limpiar búsqueda -->
                                    <Button Grid.Column="2" 
                                            Content="✕" 
                                            Command="{Binding ClearProcessSearchCommand}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Foreground="{DynamicResource SecondaryTextBrush}"
                                            FontSize="14"
                                            Padding="5,0"
                                            Cursor="Hand"
                                            Visibility="{Binding ProcessSearchText, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Border>
                            
                            <!-- Botón Actualizar -->
                            <Button Grid.Column="2" 
                                    Content="Actualizar Lista" 
                                    Command="{Binding RefreshProcessesCommand}" 
                                    Style="{StaticResource SecondaryButtonStyle}" 
                                    Padding="15,8"/>
                        </Grid>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Source={StaticResource GroupedProcesses}}" AutoGenerateColumns="False" 
                                  Background="Transparent" Foreground="{DynamicResource TextBrush}" BorderThickness="0" 
                                  CanUserAddRows="False" SelectionMode="Single"
                                  VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
                            <DataGrid.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.ContainerStyle>
                                        <Style TargetType="{x:Type GroupItem}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                                        <Expander IsExpanded="True" BorderBrush="#222" BorderThickness="0,0,0,1" Margin="0,0,0,10">
                                                            <Expander.Header>
                                                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                                                    <TextBlock Text="{Binding Name, Converter={StaticResource IsForegroundToTypeConverter}, ConverterParameter='Aplicaciones en Primer Plano|Procesos en Segundo Plano'}" 
                                                                               FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" FontSize="14"/>
                                                                    <TextBlock Text="{Binding ItemCount, StringFormat=' ({0})'}" 
                                                                               Foreground="#888" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                                </StackPanel>
                                                            </Expander.Header>
                                                            <ItemsPresenter Margin="20,0,0,0"/>
                                                        </Expander>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </GroupStyle.ContainerStyle>
                                </GroupStyle>
                            </DataGrid.GroupStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70"/>
                                <DataGridTextColumn Header="Nombre del Proceso" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Memoria" Binding="{Binding WorkingSetMb, StringFormat={}{0:F1} MB}" Width="150"/>
                                <DataGridTemplateColumn Header="Acciones" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="DETENER"
                                                    Command="{Binding DataContext.KillProcessCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource DangerButtonStyle}"
                                                    Padding="15,4"
                                                    FontSize="10"
                                                    ToolTip="Finalizar este proceso"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Servicios -->
            <TabItem Header="Servicios">
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="0,12,12,12" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Barra de búsqueda y botón actualizar -->
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Cuadro de búsqueda -->
                            <Border Grid.Column="0" 
                                    Background="{DynamicResource WindowBackgroundBrush}" 
                                    BorderBrush="{DynamicResource SecondaryTextBrush}" 
                                    BorderThickness="1" 
                                    CornerRadius="6"
                                    Padding="10,8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Width" Value="200"/>
                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="300" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="200" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Ícono de búsqueda -->
                                    <TextBlock Grid.Column="0" 
                                               Text="🔍" 
                                               FontSize="14" 
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>
                                    
                                    <!-- TextBox de búsqueda -->
                                    <TextBox Grid.Column="1" 
                                             Text="{Binding ServiceSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{DynamicResource TextBrush}"
                                             VerticalAlignment="Center"
                                             FontSize="13">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Style.Triggers>
                                                    <Trigger Property="Text" Value="">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                                    <VisualBrush.Visual>
                                                                        <TextBlock Text="Buscar servicio por nombre..." 
                                                                                   Foreground="White" 
                                                                                   FontSize="13"
                                                                                   Opacity="0.6"/>
                                                                    </VisualBrush.Visual>
                                                                </VisualBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    
                                    <!-- Botón limpiar búsqueda -->
                                    <Button Grid.Column="2" 
                                            Content="✕" 
                                            Command="{Binding ClearServiceSearchCommand}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Foreground="{DynamicResource SecondaryTextBrush}"
                                            FontSize="14"
                                            Padding="5,0"
                                            Cursor="Hand"
                                            Visibility="{Binding ServiceSearchText, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Border>
                            
                            <!-- Botón Actualizar -->
                            <Button Grid.Column="2" 
                                    Content="Actualizar Lista" 
                                    Command="{Binding RefreshServicesCommand}" 
                                    Style="{StaticResource SecondaryButtonStyle}" 
                                    Padding="15,8"/>
                        </Grid>
                        
                         <DataGrid Grid.Row="1" ItemsSource="{Binding WindowsServices}" AutoGenerateColumns="False" Background="Transparent" Foreground="{DynamicResource TextBrush}" BorderThickness="0" CanUserAddRows="False"
                               VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding DisplayName}" Width="*"/>
                            <DataGridTextColumn Header="Estado" Binding="{Binding Status}" Width="100"/>
                            <DataGridTemplateColumn Header="Control" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Button Content="Iniciar" 
                                                    Command="{Binding DataContext.StartServiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding Name}" 
                                                    Visibility="{Binding IsRunning, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" 
                                                    Style="{StaticResource SecondaryButtonStyle}" 
                                                    Padding="15,2"
                                                    MinWidth="80"/>
                                            <Button Content="Detener" 
                                                    Command="{Binding DataContext.StopServiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding Name}" 
                                                    Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                                    Style="{StaticResource DangerButtonStyle}" 
                                                    Padding="15,2"
                                                    MinWidth="80"/>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    </Grid>
                </Border>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
