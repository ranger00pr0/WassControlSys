<UserControl x:Class="WassControlSys.Views.ThermalMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Monitor Térmico" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,30"/>
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" HorizontalAlignment="Left" MinWidth="400">
            <StackPanel>
                <TextBlock Text="Temperatura CPU" Foreground="White" FontSize="18" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CpuTempC, StringFormat={}{0:F1} °C}" Foreground="White" FontSize="24"/>
                    <TextBlock Text="{Binding ThermalAlert}" Foreground="#FF5540" FontSize="16" Margin="15,8,0,0"/>
                </StackPanel>
                <Button Content="Actualizar" Command="{Binding RefreshThermalCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,15,0,0"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>

