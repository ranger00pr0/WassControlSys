<UserControl x:Class="WassControlSys.Views.StartupManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:WassControlSys.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Style="{StaticResource AnimatedViewStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Administrador de Inicio" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,30"/>

        <Border Grid.Row="1" Style="{StaticResource AnimatedCardStyle}">
            <StackPanel>
                <TextBlock Text="Aplicaciones al iniciar Windows" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                
                <ListView ItemsSource="{Binding StartupItems}" Background="Transparent" BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:StartupItem}">
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Path}" Foreground="#888" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Type}" Foreground="#666" FontSize="10"/>
                                </StackPanel>

                                <ToggleButton Grid.Column="1" 
                                              IsChecked="{Binding IsEnabled}" 
                                              Content="{Binding IsEnabled, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Activado|Desactivado'}" 
                                              Style="{StaticResource ToggleSwitchStyle}" Margin="10,0"/>

                                <Button Grid.Column="2" Content="Deshabilitar" Style="{StaticResource SecondaryButtonStyle}" Margin="5,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
