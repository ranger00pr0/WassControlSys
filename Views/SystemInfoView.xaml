<UserControl x:Class="WassControlSys.Views.SystemInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="0,0,0,0">
        <TextBlock Text="Información del Sistema" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,30"/>

        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="30" HorizontalAlignment="Left" MinWidth="500">
             <StackPanel>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Equipo:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.MachineName}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1"/>
                </Grid>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Sistema Operativo:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.OsVersion}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Procesador:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.Processor}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1" TextWrapping="Wrap"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Memoria RAM:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.TotalRam}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Tarjeta Gráfica:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.Gpu}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1" TextWrapping="Wrap"/>
                </Grid>
                
                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Disco del Sistema:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.SystemDisk}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1"/>
                </Grid>

                <Grid Margin="0,20,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="BIOS:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.BiosVersion}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Red:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.NetworkInfo}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1" TextWrapping="Wrap"/>
                </Grid>

                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Tiempo Activo:" Foreground="#888" FontSize="14" Grid.Column="0"/>
                    <TextBlock Text="{Binding SystemInformation.Uptime}" Foreground="White" FontSize="14" FontWeight="SemiBold" Grid.Column="1"/>
                </Grid>
             </StackPanel>
        </Border>

        <TextBlock Text="Herramientas de Backup" Style="{StaticResource HeaderTextStyle}" FontSize="18" Margin="0,30,0,15"/>
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" HorizontalAlignment="Left" MinWidth="500">
            <StackPanel>
                <TextBlock Text="Copia de Seguridad de Controladores" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,5"/>
                <TextBlock Text="Exporta todos los controladores de terceros instalados a una carpeta en el Escritorio. Útil para reinstalaciones de Windows." Foreground="#888" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,15"/>
                <Button Content="Exportar Controladores" Command="{Binding ExportDriversCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
