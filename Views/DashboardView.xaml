<UserControl x:Class="WassControlSys.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Dashboard" Style="{StaticResource HeaderTextStyle}"/>
            <TextBlock Text="{Binding WelcomeMessage}" Style="{StaticResource SubHeaderTextStyle}" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Column: Quick Actions + Mode Selector -->
            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                 <!-- Quick Actions Card -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Acciones Rápidas" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                        <Button Content="Limpiar Archivos Temporales" Command="{Binding CleanTempFilesCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left"/>
                        <TextBlock Text="Libera espacio eliminando caché y temporales." Foreground="#888" FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Selector de Modo Card (Task 01) -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Modo de Rendimiento" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton Content="General" 
                                         IsChecked="{Binding CurrentMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=General}" 
                                         Command="{Binding ApplyPerformanceModeCommand}" CommandParameter="General"
                                         Foreground="White" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                            <RadioButton Content="Gamer" 
                                         IsChecked="{Binding CurrentMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Gamer}" 
                                         Command="{Binding ApplyPerformanceModeCommand}" CommandParameter="Gamer"
                                         Foreground="White" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                            <RadioButton Content="Dev" 
                                         IsChecked="{Binding CurrentMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dev}" 
                                         Command="{Binding ApplyPerformanceModeCommand}" CommandParameter="Dev"
                                         Foreground="White" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                            <RadioButton Content="Oficina" 
                                         IsChecked="{Binding CurrentMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Oficina}" 
                                         Command="{Binding ApplyPerformanceModeCommand}" CommandParameter="Oficina"
                                         Foreground="White" Margin="0,0,0,0" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right Column: System Monitor (Task 04) -->
            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="15,0,0,0" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Monitor del Sistema" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                    
                    <!-- CPU -->
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto" MinWidth="40"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="CPU" Foreground="#AAA" Grid.Column="0"/>
                        <ProgressBar Grid.Column="1" Value="{Binding CpuUsage}" Maximum="100" Height="10" Margin="10,0" Background="#333" BorderThickness="0"/>
                        <TextBlock Text="{Binding CpuUsage, StringFormat={}{0:F0}%}" Foreground="White" Grid.Column="2" HorizontalAlignment="Right"/>
                    </Grid>

                    <!-- RAM -->
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto" MinWidth="40"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="RAM" Foreground="#AAA" Grid.Column="0"/>
                        <ProgressBar Grid.Column="1" Value="{Binding RamUsage}" Maximum="100" Height="10" Margin="10,0" Background="#333" BorderThickness="0"/>
                        <TextBlock Text="{Binding RamUsage, StringFormat={}{0:F0}%}" Foreground="White" Grid.Column="2" HorizontalAlignment="Right"/>
                    </Grid>

                    <!-- Disk -->
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto" MinWidth="40"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Disco" Foreground="#AAA" Grid.Column="0"/>
                        <ProgressBar Grid.Column="1" Value="{Binding DiskUsage}" Maximum="100" Height="10" Margin="10,0" Background="#333" BorderThickness="0"/>
                        <TextBlock Text="{Binding DiskUsage, StringFormat={}{0:F0}%}" Foreground="White" Grid.Column="2" HorizontalAlignment="Right"/>
                    </Grid>

                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>